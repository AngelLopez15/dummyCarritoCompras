// imr para crear rapido el import
import React from 'react';

// sfc para crear rapida la funcion
const Footer = ({fecha}) => {
    return ( 
        <footer>
            <p>Todos los derechos reservados &copy; {fecha}</p>
        </footer>
    );
}

export default Footer;